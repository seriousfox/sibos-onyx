// Generated by CoffeeScript 1.7.1

/**
 * * Function to start a progressbar
 * * @param {callback} Fired once the animation is complete
 * Example
new App.progress(element).start ->
  alert 'completed'
 */
App.progress = (function() {
  function progress(element) {
    this.$el = $(element);
    this.$meter = this.$el.find('.meter');
    return this;
  }

  progress.prototype.start = function(callback) {
    this.$meter.css({
      'width': 0
    });
    return this.$meter.animate({
      width: "+=100%"
    }, 3000, callback);
  };

  return progress;

})();
